/* Calculator-specific V2 logic. Shared helpers live in /assets/js/rv-tool-core.js. */
const form = document.getElementById('calculatorForm');
    const presetButtons = document.querySelectorAll('[data-preset]');
    const outputs = {
      netIncome: document.getElementById('netIncome'), grossIncomeOut: document.getElementById('grossIncomeOut'), socialInsurance: document.getElementById('socialInsurance'), healthInsurance: document.getElementById('healthInsurance'), summaryLimit: document.getElementById('summaryLimit'), taxMode: document.getElementById('taxMode'), taxAfterCredit: document.getElementById('taxAfterCredit'), statusBadge: document.getElementById('statusBadge'), resultNote: document.getElementById('resultNote'), breakdownBody: document.getElementById('breakdownBody'), limitStatus: document.getElementById('limitStatus'), limitText: document.getElementById('limitText'), taxStatus: document.getElementById('taxStatus'), taxText: document.getElementById('taxText'), payrollStatus: document.getElementById('payrollStatus'), nextActionText: document.getElementById('nextActionText'), limitHelper: document.getElementById('limitHelper'), limitProgressBar: document.getElementById('limitProgressBar'), meterPercent: document.getElementById('meterPercent'), meterLabel: document.getElementById('meterLabel'), stickyMobileValue: document.getElementById('stickyMobileValue'), decisionText: document.getElementById('decisionText'), heroPreviewNet: document.getElementById('heroPreviewNet'), heroPreviewGross: document.getElementById('heroPreviewGross'), heroPreviewInsurance: document.getElementById('heroPreviewInsurance'), heroPreviewTax: document.getElementById('heroPreviewTax'), heroPreviewBadge: document.getElementById('heroPreviewBadge'), heroPreviewType: document.getElementById('heroPreviewType')
    };
    const formatCurrency = value => new Intl.NumberFormat('cs-CZ',{style:'currency',currency:'CZK',maximumFractionDigits:0}).format(Number.isFinite(value)?value:0);
    function getValues(){return {agreementType:document.getElementById('agreementType').value,grossIncome:Number(document.getElementById('grossIncome').value),taxRate:Number(document.getElementById('taxRate').value),signedDeclaration:document.getElementById('signedDeclaration').checked,applyTaxCredit:document.getElementById('applyTaxCredit').checked};}
    function getLimit(type){return type === 'DPP' ? 12000 : 4500;}
    function getTaxCredit(values){return values.signedDeclaration && values.applyTaxCredit ? 2570 : 0;}
    function calculateAgreement(values){const limit=getLimit(values.agreementType);const insuranceApplies=values.grossIncome>=limit;const socialInsurance=Math.round(values.grossIncome*(insuranceApplies?0.071:0));const healthInsurance=Math.round(values.grossIncome*(insuranceApplies?0.045:0));const taxBeforeCredit=Math.round(values.grossIncome*(values.taxRate/100));const taxCredit=Math.min(getTaxCredit(values),taxBeforeCredit);const taxAfterCredit=Math.max(0,taxBeforeCredit-taxCredit);const taxMode=(!values.signedDeclaration && values.grossIncome < limit)?'Srážková daň':'Zálohová daň';const netIncome=values.grossIncome-socialInsurance-healthInsurance-taxAfterCredit;return {limit,insuranceApplies,socialInsurance,healthInsurance,taxBeforeCredit,taxCredit,taxAfterCredit,taxMode,netIncome};}
    function renderBreakdown(values,result){const rows=[['Hrubá odměna','Zadaná hrubá měsíční odměna.',values.grossIncome],['Sociální pojištění',result.insuranceApplies?'Odvod sociálního pojištění zaměstnance.':'Pod rozhodným limitem se v modelu neodvádí.',-result.socialInsurance],['Zdravotní pojištění',result.insuranceApplies?'Odvod zdravotního pojištění zaměstnance.':'Pod rozhodným limitem se v modelu neodvádí.',-result.healthInsurance],['Daň před slevou','Orientační daň ze zadané hrubé odměny.',-result.taxBeforeCredit],['Sleva na poplatníka',values.signedDeclaration&&values.applyTaxCredit?'Uplatněná základní sleva na poplatníka.':'Sleva se neuplatňuje.',result.taxCredit],['Daň po slevě','Skutečně odečtená orientační daň po slevě.',-result.taxAfterCredit],['Čistá odměna','Orientační částka k výplatě.',result.netIncome]];outputs.breakdownBody.innerHTML=rows.map(r=>`<tr><td>${r[0]}<br><small>${r[1]}</small></td><td>${r[2]>=0?formatCurrency(r[2]):'- '+formatCurrency(Math.abs(r[2]))}</td></tr>`).join('');}
    function updateDecision(values,result){const diff=values.grossIncome-result.limit;const near=Math.abs(diff)<=1500;outputs.limitStatus.className='tag '+(result.insuranceApplies?'caution':'safe');outputs.limitStatus.textContent=result.insuranceApplies?'Nad limitem':'Pod limitem';outputs.limitText.textContent=result.insuranceApplies?`Odvody už vznikají. Aktuálně jste přibližně o ${formatCurrency(Math.max(0,diff))} nad limitem.`:`Odvody zatím nevznikají. Do limitu zbývá přibližně ${formatCurrency(Math.max(0,-diff))}.`;outputs.taxStatus.className='tag '+(values.signedDeclaration?'safe':'caution');outputs.taxStatus.textContent=values.signedDeclaration?'Sleva pomáhá':'Bez slevy';outputs.taxText.textContent=values.signedDeclaration?'Podepsané Prohlášení může čistou odměnu výrazně zlepšit.':'Bez Prohlášení se neuplatní základní sleva na poplatníka.';outputs.payrollStatus.className='tag '+(near?'caution':result.insuranceApplies?'risk':'safe');outputs.payrollStatus.textContent=near?'Blízko limitu':result.insuranceApplies?'Vyšší srážky':'Příznivý režim';outputs.nextActionText.textContent=near?'Jste blízko rozhodného limitu. Zkuste si porovnat částku těsně pod a nad limitem.':result.insuranceApplies?'Porovnejte, jestli se vyšší hrubá odměna po odvodech stále vyplatí.':'Pokud řešíte nabídku práce, porovnejte i DPČ nebo HPP podle rozsahu práce.';outputs.decisionText.textContent=result.insuranceApplies?'Výpočet ukazuje scénář, ve kterém se do čisté odměny propisují sociální a zdravotní odvody. Proto je důležité nesledovat jen hrubou částku.':'Výpočet ukazuje scénář pod rozhodným limitem. Čistý výsledek je často příznivý, zejména pokud je podepsané Prohlášení a uplatní se sleva.';outputs.resultNote.textContent=!values.signedDeclaration&&!result.insuranceApplies?'Orientačně vychází zdanění srážkovou daní bez uplatnění základní slevy na poplatníka.':!values.signedDeclaration&&result.insuranceApplies?'Bez Prohlášení a při vzniku odvodů počítá model zálohovou daň bez základní slevy na poplatníka.':'Výsledek zohledňuje podpis Prohlášení a základní slevu na poplatníka. Konkrétní situaci může ovlivnit souběh dalších příjmů.';}
    function updateLimitUI(values,result){const percent=Math.min(160,Math.max(0,(values.grossIncome/result.limit)*100));outputs.limitProgressBar.style.width=Math.min(100,percent)+'%';outputs.meterPercent.textContent=Math.round(percent)+' %';outputs.meterLabel.textContent=`Využití limitu ${formatCurrency(result.limit)}`;outputs.limitHelper.textContent=`Rozhodný limit pro ${values.agreementType} v zachovaném modelu: ${formatCurrency(result.limit)}.`;}
    function render(values,result){outputs.netIncome.textContent=formatCurrency(result.netIncome);outputs.grossIncomeOut.textContent=formatCurrency(values.grossIncome);outputs.socialInsurance.textContent=formatCurrency(result.socialInsurance);outputs.healthInsurance.textContent=formatCurrency(result.healthInsurance);outputs.summaryLimit.textContent=formatCurrency(result.limit);outputs.taxMode.textContent=result.taxMode;outputs.taxAfterCredit.textContent=formatCurrency(result.taxAfterCredit);outputs.stickyMobileValue.textContent=formatCurrency(result.netIncome);outputs.statusBadge.textContent=result.insuranceApplies?'Vznikají odvody':'Bez odvodů';outputs.heroPreviewNet.textContent=formatCurrency(result.netIncome);outputs.heroPreviewGross.textContent=formatCurrency(values.grossIncome);outputs.heroPreviewInsurance.textContent=formatCurrency(result.socialInsurance+result.healthInsurance);outputs.heroPreviewTax.textContent=formatCurrency(result.taxAfterCredit);outputs.heroPreviewBadge.textContent=result.insuranceApplies?'Vznikají odvody':'Bez odvodů';outputs.heroPreviewType.textContent=values.agreementType;renderBreakdown(values,result);updateDecision(values,result);updateLimitUI(values,result);}
    function syncCreditAvailability(){const signed=document.getElementById('signedDeclaration').checked;const credit=document.getElementById('applyTaxCredit');if(!signed){credit.checked=false;credit.disabled=true}else{credit.disabled=false}}
    function runCalculation(){const values=getValues();if(!values.grossIncome||values.grossIncome<=0)return;render(values,calculateAgreement(values));}
    function applyPreset(btn){document.getElementById('agreementType').value=btn.dataset.type;document.getElementById('grossIncome').value=btn.dataset.income;document.getElementById('signedDeclaration').checked=btn.dataset.signed==='1';document.getElementById('applyTaxCredit').checked=btn.dataset.credit==='1';syncCreditAvailability();presetButtons.forEach(p=>p.classList.toggle('active',p===btn));runCalculation();}
    form.addEventListener('submit',e=>{e.preventDefault();runCalculation()});['agreementType','grossIncome','taxRate','signedDeclaration','applyTaxCredit'].forEach(id=>{const el=document.getElementById(id);el.addEventListener('input',()=>{if(id==='signedDeclaration')syncCreditAvailability();runCalculation()});el.addEventListener('change',()=>{if(id==='signedDeclaration')syncCreditAvailability();runCalculation()})});presetButtons.forEach(btn=>btn.addEventListener('click',()=>applyPreset(btn)));document.getElementById('resetBtn').addEventListener('click',()=>applyPreset(presetButtons[0]));document.getElementById('copyResultBtn').addEventListener('click',async()=>{const text=`DPP / DPČ kalkulačka\nČistá odměna: ${outputs.netIncome.textContent}\nHrubá odměna: ${outputs.grossIncomeOut.textContent}\nSociální pojištění: ${outputs.socialInsurance.textContent}\nZdravotní pojištění: ${outputs.healthInsurance.textContent}\nDaňový režim: ${outputs.taxMode.textContent}`;try{await navigator.clipboard.writeText(text);outputs.statusBadge.textContent='Výsledek zkopírován'}catch(e){outputs.statusBadge.textContent='Kopírování se nepovedlo'}});syncCreditAvailability();runCalculation();
