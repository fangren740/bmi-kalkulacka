name: RV Predeploy Audit

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  static-audit:
    name: Static release gate
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run V-next state tracking gate
        id: vnext_tracking
        shell: bash
        run: |
          set -o pipefail
          python rv-vnext-progress-audit.py \
            --root . \
            --json-out rv-vnext-progress-report.json | tee rv-vnext-progress-report.txt

      - name: Run deterministic audit
        if: ${{ !cancelled() }}
        id: audit
        shell: bash
        run: |
          set -o pipefail
          python .github/scripts/audit_static_site.py \
            --root . \
            --config audits/audit-config.json \
            --check-js \
            --json-out rv-audit-report.json | tee rv-audit-report.txt

      - name: Run V-next accessibility and brand contract
        if: ${{ !cancelled() }}
        shell: bash
        run: |
          set -o pipefail
          python rv-vnext-a11y-lint.py --all-vnext | tee rv-a11y-report.txt

      - name: Check complete calculator inventory
        if: ${{ !cancelled() }}
        run: python .github/scripts/vnext_inventory.py --check

      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rv-static-audit-report
          path: |
            rv-vnext-progress-report.json
            rv-vnext-progress-report.txt
            rv-audit-report.json
            rv-audit-report.txt
            rv-a11y-report.txt
          if-no-files-found: warn
          retention-days: 30
