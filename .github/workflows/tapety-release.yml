name: Tapety release integration

on:
  push:
    branches:
      - release/kalkulacka-tapet-20260909

permissions:
  contents: write

jobs:
  integrate-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout release branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prepare integrator for current registry semantics
        run: python .tmp-tapety/patch_integrator.py

      - name: Normalize catalogue count handling
        run: python .tmp-tapety/patch_catalog_counts.py

      - name: Integrate approved release candidate
        run: python .tmp-tapety/integrate.py

      - name: Candidate syntax and registry JSON
        run: |
          node --check tapety-core.js
          node --check tapety-page.js
          python -m json.tool calculators-registry.json >/dev/null

      - name: Static site audit
        run: python .github/scripts/audit_static_site.py --root . --config audits/audit-config.json --check-js --json-out /tmp/rv-static.json

      - name: V-next tracking audit
        run: python rv-vnext-progress-audit.py --root . --json-out /tmp/rv-tracking.json

      - name: V-next accessibility lint
        run: python rv-vnext-a11y-lint.py --all-vnext

      - name: Inventory check
        run: python .github/scripts/vnext_inventory.py --check

      - name: Commit integrated release
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git diff --cached --quiet && { echo "No changes to commit"; exit 1; }
          git commit -m "Release calculator: wallpaper roll and cutting plan"
          git push origin HEAD:${GITHUB_REF_NAME}
